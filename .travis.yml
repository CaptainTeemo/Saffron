language: swift
osx_image: xcode7
xcode_project: Saffron.xcodeproj
xcode_scheme: Saffron
script:
  - xctool -project Saffron.xcodeproj -scheme Saffron build test -sdk iphonesimulator GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES
after_success:
  - bash <(curl -s https://codecov.io/bash)
